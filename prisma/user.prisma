model User {
  id                 String   @id @default(cuid())
  username           String   @unique
  password           String   @unique
  maxTokensPerDay    Int      @default(100)
  tokensSpentCount   Int      @default(0)

  createdAt DateTime @default(now())

  toDoLists      ToDoList[]
  sessions       Session[]
}
